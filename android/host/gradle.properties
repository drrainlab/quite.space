android.useAndroidX=true
android.nonTransitiveRClass=true
org.gradle.jvmargs=-Xmx2g
# The rig is arm64 only: every device AR-0 measures on is arm64-v8a, and a
# universal APK would just make the artifact bigger for no measurement.
android.defaults.buildfeatures.buildconfig=true
